<div class="row">
     <div class="col-sm- 4">
          <h4 class="heading">Full Products</h4>
               <table class="table table-striped">
                    <thead>
                         <tr>
                              <th>Product and pattern</th>
                              <th>Action</th>
                         </tr>
                    </thead>
               </table>
          <div id="productdescs">
               <table class="table table-striped">
                    <tbody>
                         <% @proddesc.each do |pd| %>
                              <tr>
                                   <td class="text-capitalize"><%= pd.product.name %> in <%= pd.pattern.name %></td>
                                   <td>
                                   <!-- Actions -->
                                        <!-- Delete -->
                                        <form class="" action="<%= delete_productdesc_path %>" method="post">
                                             <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token%>"/>
                                             <input type="hidden" name="product_desc[product_desc_id]" value="<%= pd.id%>"/>
                                             <input type="hidden" name="_method" value="delete"/>
                                             <button class="btn btn-sm btn-warning">Remove</button></p>
                                        </form>
                                   </td>
                              </tr>
                         <% end %>
                    </tbody>
               </table>
          </div>
     </div>
</div>
